==============================================================================
------------------------------------------------------------------------------
                                                           *NeotestBun.toggle()*
                             `NeotestBun.toggle`()
Toggle the plugin by calling the `enable`/`disable` methods respectively.

------------------------------------------------------------------------------
                                                           *NeotestBun.enable()*
                          `NeotestBun.enable`({scope})
Initializes the plugin, sets event listeners and internal state.

------------------------------------------------------------------------------
                                                          *NeotestBun.disable()*
                             `NeotestBun.disable`()
Disables the plugin, clear highlight groups and autocmds, closes side buffers and resets the internal state.


==============================================================================
------------------------------------------------------------------------------
                                                            *NeotestBun.options*
                              `NeotestBun.options`
NeotestBun configuration with its default values.

Type ~
`(table)`
Default values:
>lua
  NeotestBun.options = {
    -- Prints useful logs about what event are triggered, and reasons actions are executed.
    debug = false,
  }

<
------------------------------------------------------------------------------
                                                            *NeotestBun.setup()*
                         `NeotestBun.setup`({options})
Define your neotest-bun setup.

Parameters ~
{options} `(table)` Module config table. See |NeotestBun.options|.

Usage ~
`require("neotest-bun").setup()` (add `{}` with your |NeotestBun.options| table)


==============================================================================
------------------------------------------------------------------------------
                                                            *bun.isBunProject()*
                              `bun.isBunProject`()
Return ~
`(boolean)`


==============================================================================
------------------------------------------------------------------------------
                                                                     *stripWS()*
                             `stripWS`({self}, {s})
Removes whitespaces

------------------------------------------------------------------------------
Parse a Normal tag

------------------------------------------------------------------------------
Get next tag (first pass - fix exceptions below).


==============================================================================
------------------------------------------------------------------------------
prints a table in an easy-to-ready format

------------------------------------------------------------------------------
to generate a string prepresentation of a table

------------------------------------------------------------------------------
Loads an XML file from a specified path

------------------------------------------------------------------------------
an _attr element from a table that represents the attributes of an XML tag,

------------------------------------------------------------------------------
                                                              *getSingleChild()*
                             `getSingleChild`({tb})
the first key of a given table

------------------------------------------------------------------------------
a Lua table to a XML String representation.


==============================================================================
------------------------------------------------------------------------------
@module XML Tree Handler.

------------------------------------------------------------------------------
a new handler object.

------------------------------------------------------------------------------
Recursively removes redundant vectors for nodes

------------------------------------------------------------------------------
If an object is not an array,

------------------------------------------------------------------------------
a start tag.

------------------------------------------------------------------------------
an end tag.

------------------------------------------------------------------------------
a tag content.

------------------------------------------------------------------------------
                                                                    *tree.cdata*
                                  `tree.cdata`
CDATA tag content.


 vim:tw=78:ts=8:noet:ft=help:norl: